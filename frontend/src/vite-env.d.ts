/// <reference types="vite/client" />
// Vite's ambient types, so a stylesheet or asset import is a known module rather than an error.
// The reference resolves inside the already-installed `vite` package; nothing new is added.
